<nav class="navbar navbar-expand-md bg-white border-bottom sticky-top" style="box-shadow: 0 1px 2px rgba(0,0,0,0.08);">
    <div class="container-fluid" style="max-width: 1760px; padding: 0 2rem;">
        <a class="navbar-brand" href="/listings" style="font-size: 1.5rem; font-weight: 700; color: #ff385c;">
            <i class="fa-solid fa-house-chimney"></i> Come and Live
        </a>
        
        <!-- Search Bar (Center) -->
        <form action="/listings" method="GET" 
      class="d-none d-lg-flex"
      style="position: absolute; left: 50%; transform: translateX(-50%);">

  <div style="border: 1px solid #dddddd; border-radius: 40px; padding: 0.5rem 1rem; display: flex; align-items: center; gap: 1rem; box-shadow: 0 1px 2px rgba(0,0,0,0.08); transition: box-shadow 0.2s;">

      <!-- Location Search -->
      <input 
          type="text" 
          name="search"
          placeholder="Search destinations..."
          style="border: none; outline: none; font-size: 0.875rem; width: 250px; background: transparent;">

      <!-- Search Button -->
      <button type="submit"
          style="background: #ff385c; border-radius: 50%; width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; border: none; cursor: pointer;">
          <i class="fa-solid fa-magnifying-glass" style="color: white; font-size: 0.875rem;"></i>
      </button>

  </div>

</form>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav ms-auto" style="gap: 1rem; align-items: center;">
                <a class="nav-link" href="/listings/new" style="font-weight: 600; color: #222222;">
                    Create New Hosting
                </a>
                
                <% if (!currentUser) { %>
                    <a class="nav-link" href="/signup" style="color: #222222; font-weight: 500;">Sign Up</a>
                    <a class="nav-link" href="/login" style="color: #222222; font-weight: 500;">Log In</a>
                <% } %>

                <% if (currentUser) { %>
                    <div style="border: 1px solid #dddddd; border-radius: 21px; padding: 0.375rem 0.75rem; display: flex; align-items: center; gap: 0.75rem; cursor: pointer; transition: box-shadow 0.2s;">
                        <i class="fa-solid fa-bars" style="color: #717171;"></i>
                        <div style="width: 30px; height: 30px; border-radius: 50%; background: linear-gradient(135deg, #667eea, #764ba2); display: flex; align-items: center; justify-content: center; color: white; font-weight: 600; font-size: 0.875rem;">
                            <%= currentUser.username.charAt(0).toUpperCase() %>
                        </div>
                    </div>
                    <a class="nav-link" href="/logout" style="color: #222222; font-weight: 500;">Log Out</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>